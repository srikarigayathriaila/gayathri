class Solution:
    def isValid(self, s):
        stack = []
        pair = {')': '(', '}': '{', ']': '['}
        for ch in s:
            if ch in pair.values():
                stack.append(ch)
            elif ch in pair:
                if not stack or stack.pop() != pair[ch]:
                    return False
        return not stack
